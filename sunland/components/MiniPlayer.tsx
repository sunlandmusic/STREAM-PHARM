import React from 'react';
import { StyleSheet, View } from 'react-native';

// This component is now empty as we're removing the mini player
// We're keeping the file to avoid import errors elsewhere
const MiniPlayer: React.FC = () => {
  return null;
};

export default MiniPlayer;